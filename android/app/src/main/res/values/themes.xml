<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Compose draws everything; this exists only so the window has the right background
      before the first frame and so AppCompatActivity has an AppCompat ancestor.
    -->
    <style name="Theme.Kpasskey" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
